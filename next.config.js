/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  trailingSlash: true,
  basePath: "/Test-deep",
  assetPrefix: "/Test-deep", 
}

module.exports = nextConfig
